import CRUD.Create;
import CRUD.Delete;
import JDBC.connection;
import Models.Tribble;

import java.sql.Connection;

public class Driver {
  public static void main(String[] args) {
//      Create.createLab(1, "Johnson");

//      Tribble temp = new Tribble(1, "hello");
//      Delete.deleteTribble(temp);
  }
}
